% \DeclareFixedFont{\ttb}{T1}{txtt}{bx}{n}{9} % for bold
% \DeclareFixedFont{\ttm}{T1}{txtt}{m}{n}{9}  % for normal

\lstset{language=Python,%
    basicstyle=\scriptsize\ttfamily,
    breaklines=true,%
    keywordstyle=\bfseries\color{scharlaken}, % e.g. import
    identifierstyle=\color{black},%
    stringstyle=\color{tuepurple},
    commentstyle=\color{gray},%
    % showstringspaces=false,%without this there will be a symbol in the places where there is a space
    numbers=left,%
    numberstyle={\tiny \color{black}},% size of the numbers
    %     numbersep=-2pt, % this defines how far the numbers are from the text
    %     emph=[1]{for,end,break},emphstyle=[1]\color{red}, %some words to emphasise
    emph=[1]{print,dir,help},
    emphstyle=[1]{\bfseries\color{scharlaken}},    % Custom highlighting style
    emph=[2]{numpy,scipy,matplotlib,pandas,plotly},
    emphstyle=[2]{\bfseries\color{examgreen}},
    emph=[3]{ones,int,str2double,long,single,simplify,diff,log,atan,solve,vpa,syms,doc,int,simplify,diff,log,atan,syms,interp3,interpn,histogram,ribbon,contourf,fzero,feval,fminsearch,fsolve,fminbnd,ezplot,varargin,optimset,odeset,ode15s,plotyy,ones,linprog,cftool,optimset,lsqnonlin,inv}, 
    emphstyle=[3]{},
    backgroundcolor=\color{gray!15},frame=tlbr, framerule=0pt,
    escapeinside={(*@}{@*)}
    }

\lstdefinestyle{output}{
    basicstyle=\scriptsize\ttfamily,
    breaklines=true,%
    keywordstyle=\bfseries\color{scharlaken}, % e.g. import
    identifierstyle=\color{black},%
    stringstyle=\color{tuepurple},
    commentstyle=\color{gray},%
    % showstringspaces=false,%without this there will be a symbol in the places where there is a space
    numbers=none,%
    backgroundcolor=\color{white},
    frame=shadowbox, framerule=1pt,
    escapeinside={(*@}{@*)}
}